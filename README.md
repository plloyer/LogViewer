# LogViewer

A simple, real-time log viewer for Windows, designed to monitor and filter log files (built for Knights of Honor II modding).

## Features
- Real-time log monitoring.
- Tag coloring ([Economy], [Military], etc.).
- Include and Exclude filters (supports multiple terms with `;`).
- Automatic prefix stripping.
- Persistent settings (filters and window position).
